const { PrismaClient } = require('./prisma/generated-client-v2'); const prisma = new PrismaClient(); const fs = require('fs'); async function main() { const cA1 = JSON.parse(fs.readFileSync('../ai_core/config/Ventas_WA/config_a1.json')); const cA2 = JSON.parse(fs.readFileSync('../ai_core/config/Ventas_WA/config_a2.json')); await prisma.channel.update({ where: { instanceName: 'Ventas_WA' }, data: { configA1: cA1, configA2: cA2 } }); console.log('Prisma updated.'); } main().finally(() => prisma.$disconnect());
